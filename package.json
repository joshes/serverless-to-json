{
  "name": "serverless-to-json",
  "version": "1.0.2",
  "description": "Exports a serverless configuration to Json on the terminal.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Joshua Hansen",
  "license": "MIT",
  "dependencies": {
  }
}
